# Slint Workshop Demo for WebAssembly

Install `wasm-pack`:

```sh
cargo install wasm-pack
```

Compile with `wasm-pack`:

```sh
wasm-pack build --release --target web
```

Run web browser:

```sh
python3 -m http.server
```
