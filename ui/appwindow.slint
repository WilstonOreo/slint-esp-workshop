import { Button, VerticalBox , AboutSlint } from "std-widgets.slint";

export component AppWindow inherits Window {
    width: 320px;
    height: 240px;
    in-out property<float> temperature: 0.0;
    in-out property<float> humidity: 0.0;
    
    VerticalBox {
        Text {
            text: "Temperature: \{root.temperature}°C";
            font-size: 16px;
        }
    
        Text {
            text: "Humidity: \{root.humidity}%";
            font-size: 16px;
        }
    
        AboutSlint { }
     }
}