import { TabWidget } from "std-widgets.slint";

import { ViewModel } from "logic.slint";

import { StatusBar, TitleBar } from "widgets.slint";

import { TemperaturePage, ForecastPage, RecordingPage } from "pages.slint";


export { ViewModel }



export component AppWindow inherits Window {
    VerticalLayout {
        TitleBar {
            text: "Slint Weather Station";
        }
        TabWidget {
            Tab {
                title: "Temperature";

                TemperaturePage {
                }
            }

            Tab {
                title: "Forecast";

                ForecastPage {
                }
            }

            Tab {
                title: "Recording";
                RecordingPage {

                }
            }
        }
        StatusBar {
            text: ViewModel.current.timestamp;
        }
    }
}
