import { TabWidget } from "std-widgets.slint";

import { ViewModel } from "logic.slint";

import { StatusBar, TitleBar } from "widgets.slint";

import { TemperaturePage, ScreenBrightnessPage, RecordingPage } from "pages.slint";

export { ViewModel }


export component AppWindow inherits Window {
    callback screen-brightness-slider-changed <=> screen-brightness-page.screen-brightness-changed;

    VerticalLayout {
        TitleBar {
            text: "Slint Weather Station";
        }
        TabWidget {
            Tab {
                title: "Temperature";

                TemperaturePage {

                }
            }

            Tab {
                title: "Brightness";

                screen-brightness-page := ScreenBrightnessPage {
                }
            }

            Tab {
                title: "Recording";

                RecordingPage {

                }
            }
        }
        StatusBar {
            text: ViewModel.current.timestamp;
        }
    }
}
